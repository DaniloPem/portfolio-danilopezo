import { Primeiras3LetrasPipe } from './primeiras-3-letras.pipe';

describe('Primeiras3LetrasPipe', () => {
  it('create an instance', () => {
    const pipe = new Primeiras3LetrasPipe();
    expect(pipe).toBeTruthy();
  });
});
