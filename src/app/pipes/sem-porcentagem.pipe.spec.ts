import { SemPorcentagemPipe } from './sem-porcentagem.pipe';

describe('SemPorcentagemPipe', () => {
  it('create an instance', () => {
    const pipe = new SemPorcentagemPipe();
    expect(pipe).toBeTruthy();
  });
});
